name: "check-laziness"
pipeline:
  - transformer: "Distinct"  # w/o params
    lazy: True

  - transformer: "AddLiterals"
    lazy: True
    params:
      data:
        - { "value": "lazy", "alias": "c3" }

  - transformer: "AddLiterals"
    lazy: True
    params:
      data:
        - { "value": "__ns__my_key", "alias": "c4" }
